export type Hypotenuse = {
    opposite: number;
    adjacent: number;
};

export type Opposite = {
    hypotenuse: number;
    adjacent: number;
};

export type Adjacent = {
    hypotenuse: number;
    opposite: number;
};

export type Result = {
    result: number;
};